const chai = require('chai')
const assert = chai.assert

const tunnel = require(process.cwd()+'/modules/Tunnel')

describe('Test.Tunnel', function () {

  it('should execute OK', function (done) {
    console.log('OK')
    done()
  })
})